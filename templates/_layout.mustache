<!DOCTYPE html>
<html lang="en">
<head>
	<title>{{{page-title}}} - {{{site-title}}}</title>
	{{> head }}
</head>
<body>
	<header class="Header">
		<p class="Header__title">{{{site-title}}}</p>
		{{> nav }}
	</header>
    <div role="main">
    	<h1>{{{page-title}}}</h1>
    </div>
	{{> footer }}
</body>
</html>